# Use an official lightweight Nginx image as a parent image
FROM nginx:alpine

# Copy the HTML file into Nginx's serve directory
COPY hello-world.html /usr/share/nginx/html/index.html

# No need for extra commands, Nginx will start and serve the page
